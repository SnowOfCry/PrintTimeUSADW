# Airflow extra providers installed at image build time.
# Pin versions to avoid surprise upgrades.
apache-airflow-providers-postgres==5.12.0
apache-airflow-providers-docker==3.12.0
# pandas 2.2 only recognizes SQLAlchemy >= 2.0 connectables, but Airflow 2.9.3
# pins SQLAlchemy 1.4. Pin pandas 2.1.x so read_sql/to_sql work with the engine.
pandas==2.1.4
psycopg2-binary==2.9.9
PyYAML==6.0.2
python-dotenv==1.0.1
sqlalchemy==1.4.54
